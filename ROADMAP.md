# Roadmap — builds in the queue

The agent works continuously. Completed builds are pushed as commits; upcoming
builds are listed here. Newest first in each section.

## ✅ Shipped

| Build | Date | Notes |
|-------|------|-------|
| Landing site + Pages scaffolding | 2026-09-13 | This repo's homepage |
| Midnight Pulse dashboard | 2026-09-13 | Live mainnet stats, NightForge API |
| Ada Metrics dashboard | 2026-09-13 | Live Cardano stats, Blockchair + CoinGecko |
| Agent Escrow Protocol | 2026-09-13 | JS + Python escrow state machines, unit-tested |
| Nightwatch | 2026-09-13 | Standardized privacy-health dashboard (growth, DUST, health score), 20 unit tests |
| AgentProof explorer | 2026-09-13 | Paste-any-receipt escrow auditor — 7 re-derivable checks (proof commitments, separation of duties, accounting, audit trail) in a local browser page, 64 unit tests incl. E2E against the real escrow state machine |
| Ecosystem "tag" for communities | 2026-09-13 | Repo tagged `midnightntwrk` (exact label indexers scan) + README attribution sentence + "Built for Midnight / Built for Cardano" badge on the site + PR to the official Midnight awesome-dapps list (#189) + ready-to-post community announcements (`docs/announcements.md`) |
| Midnight DID Wallet | 2026-09-14 | Build a `did:midnight` identity + inspect any DID in-browser — pure-JS BLAKE2s-256, spec-conformant MOD1 offchain-state codec (byte-for-byte against the official spec vector `3c08b857…fc21`), self-hashing DIDs, projected W3C DID Document. Zero network calls, 25 unit tests |
| Ada Yield Tracker | 2026-09-14 | Live Cardano staking yield — network APY, reward pot, and top stake pools with per-pool net APY + saturation, computed with the official CIP-16 formula from keyless `data.cardano.org` (Koios) mainnet data, plus a delegation calculator. 25 unit tests on the CIP-16 reward math |
| Ada Yield Pool Inspector | 2026-09-14 | Paste any `pool1…` (or 56-hex) id to inspect it live: full operator metadata via keyless `pool_list` (stake, pledge, margin, fixed cost, status, epoch, relays, meta URL), a 0–100 health score with exact flags (retiring / oversaturated / zero pledge / no relays / high margin / high cost), and a CIP-16 yield ladder for six stake sizes in ADA + live CoinGecko USD. Pure bech32 pool-ID codec (28-byte, byte-for-byte against two real live pools) + health/yield modules. 55 new unit tests (80 total in the app) |
| Glacier Drop checker | 2026-09-14 | Midnight Glacier Drop NIGHT thaw — the exact 4-installment unlock schedule (every 90 days, remainder-to-last) from the official `ThawingSchedule.hs` redemption contract math, live `data.cardano.org` mainnet tip + on-chain NIGHT mint total, network thaw progress + "day N of 360", and a bech32 Cardano address validator. 30 unit tests |
| Bridge Watch | 2026-09-14 | Live Cardano ↔ Midnight bridge health — lifetime bridge ops, 24h volume, hourly 24h trend sparkline, 12h-vs-12h growth, ops-per-unique-Cardano-block (parsed from `args_summary`), recent-ops table with Midnight + Cardano block refs and age labels, Cardano epoch/slot context, and a transparent 0–100 bridge health score, all from the public keyless NightForge `/api/analytics/bridge` + `/api/analytics/overview`. 26 unit tests |
| Contract Watch | 2026-09-14 | Live Midnight smart-contract ecosystem — contract census, top-contracts leaderboard, activity mix (active / dormant / abandoned / never called), call concentration (top-1/3/10 share), 30-day deployment trend with 7d-vs-7d growth, and a deploy-events cross-check (dedupe + address sanity), all from public keyless NightForge `/api/analytics/contracts` + `/api/contracts/deployed`. Byte-exact `midnight:contract-address[v2]:` codec (0x + 30-byte ASCII tag + 32-byte digest = 126 hex). 44 unit tests, incl. exact tallies over all 150 real leaderboard rows |
| NIGHT Market Tracker | 2026-09-14 | Live Midnight NIGHT token market — price, market cap, FDV, 24h volume + turnover, 24h range, rank, ATH, 7d/14d/30d changes, 72-bar 7d chart with volatility (σ of 1-step returns), supply vs the 24B cap, and an on-chain cross-check that CoinGecko's Cardano policy id (`0691b2fe…af1fa`) matches the known NIGHT policy byte-for-byte. Transparent 0–100 market-health rubric (7 checks). Keyless CoinGecko `midnight-3` API. 26 unit tests |
| Governance Watch | 2026-09-14 | Live Midnight governance dashboard — council & technical-committee motions with running vote tallies (latest-tally-per-motion reduction of the indexer's cumulative ayes/anos events), approval/rejected/active classification against each motion's threshold, authority-membership reset timeline (deduped, most-recent-first), and a transparent 0–100 governance health score (4 named components), all from the public keyless NightForge `/api/governance`. 31 unit tests, incl. exact reproduction of every live motion's outcome |
| Block Watch | 2026-09-14 | Midnight block production, verified — the last 120 blocks' hash chain re-checked link by link in the browser (every `parent_hash` against the previous block's `hash`), block-height contiguity audit (skipped-number detection with missing counts), a 120-bar per-block gap cadence chart vs the 6s target with negative-gap flagging, Cardano epoch progress bar (labeled 5-day-span assumption), and a transparent 0–100 block health score (intact chain / contiguous heights / steady cadence / fresh tip), all from the public keyless NightForge `/api/blocks` + `/api/analytics/overview`. 28 unit tests, incl. byte-exact verification over 3 real live blocks |
| Privacy Trend Watch | 2026-09-14 | Midnight's shielded ratio measured live, hour by hour, over a switchable 1h–7d window (up to 168h) — window tx stats with the shielded ratio **recomputed from raw counts** and cross-checked (pp delta) against the API-reported value, hourly activity chart with unshielded hours flagged red, second-half-vs-first-half growth (honest "insufficient history" when short), lifetime context, and a **decoded unshielded-incident register** (block, age, unique addresses, unique tokens, value spent/created — deduped against the indexer's known double-emission). Transparent 0–100 privacy health rubric (5 named checks). All from the public keyless NightForge `/api/analytics/privacy?hours=N` + `/api/analytics/overview`. 23 unit tests, anchored on a real live 12h payload (298 tx / 297 shielded / ratio 0.9966) incl. byte-for-byte parsing of the real unshielded payload string |
| Tx Health Monitor | 2026-09-14 | How cleanly Midnight settles, over its entire life — lifetime applied vs partial-success extrinsics with the partial rate **recomputed from raw counts** and cross-checked (pp delta) against the indexer-reported value, an `applied + partial = total` integrity flag, a de-duplicated recent-partial incident register (the indexer re-emits the same tx up to 3×; dropped duplicates counted and displayed), the on-chain **D-parameter decentralization timeline** (permissioned vs registered candidate pool, genesis→now, ms/second timestamp units + newest-first ordering normalized in pure code), and a transparent 0–100 settlement health rubric (4 named checks). All from the public keyless NightForge `/api/analytics/tx-health` + `/api/governance/d-parameter`. 30 unit tests, anchored on real live payloads (256,178 tx / 8,740 partial / 3.41%; D-parameter 10→130 permissioned) incl. the triple-emitted partial tx |
| L1 Sync Watch | 2026-09-14 | How closely Midnight follows Cardano L1, right now — the highest Cardano block Midnight's on-chain observer has folded in (decoded from the explorer's raw `cNightObservation.processTokens` args, with shape-based nested-JSON parsing and a max-fold over batched multi-block event batches), **live lag vs the Koios tip in exact blocks** (seconds labeled as an estimate @20s), observation cadence (median/min/max gaps + sparkline), the token-event flow in the scan window (creates/spends with 6-decimal values), and a transparent 0–100 sync health rubric (4 named checks). All from the public keyless NightForge `/api/extrinsics` + Koios `/k/api/v1/tip`. 22 unit tests, anchored on real live `/api/extrinsics` payloads (incl. a batched multi-block event call) |
| Validator Watch | 2026-09-14 | Who is actually producing Midnight's blocks — live producer share over sampled blocks from `/api/block-producers?limit=200`, with top-1/top-3/HHI concentration **recomputed from raw block counts** and cross-checked (0.5pp rounding tolerance) against the indexer's own percentages, an HHI even-split baseline, the 6-second block cadence over 24h (`/api/analytics/block-rate?hours=24`, partial current hour excluded from the mean), and the on-chain **D-parameter journey** (permissioned 10→130, `/api/governance/d-parameter`). Transparent 0–100 decentralization rubric (4 named checks; integrity pending, not faked, when not run). 36 unit tests, anchored on real live payloads (13 producers / 14,400 sampled / HHI 0.0800; 23 complete hours at exactly 600/h) incl. the newline-in-name producer quirk |
| Reward Flow Watch | 2026-09-14 | Cardano's per-epoch monetary engine, accounted — supply vs the 45B hard cap (utilization bar), real mint per epoch sparkline, headroom consumed, unclaimed reward pot, fee income, implied claims (`mint + fees − Δpot`), and the nominal ρ (0.3%) labeled separately from the measured effective rate (≈0.16%/epoch). Two conservation identities re-checked in the browser: `supply + reserves == 45B` every epoch, and mint == headroom-decay. Transparent 0–100 engine-health rubric (4 named checks: conservation 40, mint==decay 25, pot plausibility 20, rate band 15). Official keyless `data.cardano.org` `/tip` + `/totals` (20 completed epochs, in-progress tip epoch dropped). 21 unit tests, incl. a fixture of 21 verbatim mainnet rows (epochs 635–655, fetched 2026-09-14) run through the exact runtime pipeline |
| DRep Watch | 2026-09-14 | Cardano's governance, delegated — live DRep census with every `drep1…` credential bech32-decoded in the browser per CIP-129 (key `0x22` vs script `0x23` split + register-integrity bar), per-epoch delegated-stake + active-DRep sparklines since Conway (~88× growth, 172.9M→15.22B ADA; 263→870 active DReps), DRep stake as a share of circulation (~41% live at ep 655), and a transparent 0-100 participation-health rubric (5 named checks: share 30, growth 25, census integrity 20, DRep growth 15, data present 10). Official keyless `data.cardano.org` Koios `/tip` + `/drep_epoch_summary` + `/drep_list` + `/totals` (in-progress tip epoch dropped). 39 unit tests, incl. the CIP-129 official test vector and a verbatim mainnet fixture (10 epochs, 508–655) run through the exact runtime pipeline |
| Dano Positions | 2026-09-18 | Dano Finance (Cardano, DeFi Kernel) TVL monitor — current on-chain TVL, 24h/7d/30d change, growth trend label, and 7/30/90-day average TVL cards with sparklines, all from the public keyless DefiLlama protocol API (`api.llama.fi/protocol/dano-finance`, verified 200 + CORS-open). Per-market utilization/rates labeled pending — no public API yet, no fake numbers. 13 unit tests |
| Midnight Tx Finder | 2026-09-18 | Midnight mainnet block lookup — block-height search over the latest 100 blocks, live latest-blocks feed with ages, and a network overview (blocks, extrinsics, Midnight txs, bridge ops, TPS, shielded ratio, contract deploys/calls, committee size) from the public keyless NightForge `/api/blocks` + `/api/analytics/overview`. Tx-by-hash and historical blocks labeled pending until the public indexer exposes them. 9 unit tests |
| Ada Price Thermometer | 2026-09-18 | Multi-source ADA/USD agreement — three keyless quotes (Minswap DEX aggregator `agg-api.minswap.org/aggregator/ada-price`, CoinGecko `simple/price`, DefiLlama `coins.llama.fi`) fused into a consensus (median) price, a 0–100 source-agreement score with an explicit formula, a `(max−min)/median` spread readout, a 24h warming/cooling trend from the median change, and a per-source table with Δ-vs-consensus. Failed sources shown as failed, never faked; Sundae's ADA/USDC pair documented as CPP-AMM range-liquidity (not a naive AMM price) so it is not faked. 26 unit tests |

## 🚧 Up next

1. **Nightwatch: active-address + retention** — wire in the moment the
   public indexer exposes address-level analytics.

## Principles

- Real code, real tests — no stubs.
- Live public data where an API exists; clearly-labeled snapshots otherwise.
- Every app gets a Pages page with a donation address.
